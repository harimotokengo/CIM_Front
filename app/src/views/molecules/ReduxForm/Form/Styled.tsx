import styled from 'styled-components'

export const StyledForm = styled.form`
  display: flex;
  flex-direction: column;
  overflow-y: auto;
  flex: 1;
`
// display:flex
// ・・・入力画面(Main)とキャンセル登録ボタン(footer)を縦並びにする

// flex-direction
// ・・・上側に入力画面(Main)、下側にキャンセル登録ボタン(footer)を配置
