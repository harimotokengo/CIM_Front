import { ButtonProps } from '../../atoms/Button'
import { StyledLargeButton } from './Styled'

export const LargeButton = (props: ButtonProps) => <StyledLargeButton {...props} />

export default LargeButton
